/* function makeNegative(num) {

  return (num * -1 !== num) ? num * -1 : num ;
}
console.log(makeNegative(-20)); */


function makeNegatiValues(num){
  return num > 0 ? num * -1 : num;
}
console.log(makeNegatiValues(42));